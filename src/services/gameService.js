import { addDoc, collection } from "firebase/firestore"
import { db } from "./firebase"

export const gameService = {
    // createGame,
}

// const createGame = async (date, time, maxPlayers) => {
//     await addDoc(collection(db, "games", {
//         date,
//         time,
//         maxPlayers
//     }))
// }